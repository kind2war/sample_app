<h1>編集画面</h1>
<%= form_with model: @list do |f| %>  <!-- ch7で一部記述を削除修正。routes.rbでresoucesメソッドを実装。ルーティングの自動生成をした際、記述が変わったため -->

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>
  <%= f.submit '保存' %>

<% end %>